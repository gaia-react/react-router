# Git Workflow

Never commit to `main`/`master`. Never force-push to `main`/`master`. Machine-enforced by `.claude/hooks/block-main-destructive-git.sh`. Full workflow and branch conventions: `wiki/concepts/Git Workflow.md`.